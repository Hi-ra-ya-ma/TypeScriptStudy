function taxedTs(amount): number {
    return amount * 1.1;
}

function feeTs(amount): number {
    return amount * 1.4;
}

//function price(amount): number {
//    return '${fee(amount)}'; // Error
//}